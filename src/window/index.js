import { dismiss, open, fromRight } from './PopupWindow.svelte'
import { confirm } from './Confirm.svelte'

export const Popup = {
    dismiss,
    open,
    fromRight,
}

export default {
    confirm,
}
